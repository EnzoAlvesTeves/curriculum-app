<resources>
    <!-- Base application theme changed to MaterialComponents to avoid M3 automatic tinting -->
    <style name="Base.Theme.CurriculumApp" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">@color/primary_orange</item>
        <item name="colorPrimaryVariant">@color/primary_orange</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorOnSurface">@color/black</item>
        <item name="colorOnBackground">@color/black</item>
        <item name="android:textColorPrimary">@color/black</item>
        <item name="android:textColorSecondary">@color/black</item>
        <item name="android:textColorHint">@color/text_grey</item>

        <!-- Control Surface colors (Cards, Sheets, Menus) -->
        <item name="colorSurface">@color/white</item>
        <item name="android:colorBackground">#F5F5F5</item>
        <item name="android:windowBackground">#F5F5F5</item>
        <item name="alertDialogTheme">@style/ThemeOverlay.CurriculumApp.AlertDialog</item>
    </style>

    <style name="Theme.CurriculumApp" parent="Base.Theme.CurriculumApp">
        <!-- Default style for TextInputLayout -->
        <item name="textInputStyle">@style/Widget.App.TextInputLayout</item>
        <!-- Default style for EditText -->
        <item name="editTextStyle">@style/Widget.App.EditText</item>
        <!-- Default style for Button -->
        <item name="buttonStyle">@style/Widget.App.Button</item>
    </style>

    <style name="Widget.App.TextInputLayout" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="boxBackgroundColor">@color/white</item>
        <item name="boxStrokeColor">@color/input_stroke</item>
        <item name="boxStrokeWidth">1dp</item>
        <item name="boxStrokeWidthFocused">2dp</item>
        <item name="hintTextColor">@color/text_grey</item>
    </style>

    <!-- Style for EditText with black text color -->
    <style name="Widget.App.EditText" parent="Widget.AppCompat.EditText">
        <item name="android:textColor">@color/black</item>
        <item name="android:textColorHint">@color/text_grey</item>
    </style>

    <style name="Widget.App.TextInputEditText" parent="Widget.MaterialComponents.TextInputEditText.OutlinedBox">
        <item name="android:textColor">@color/black</item>
        <item name="android:textColorHint">@color/text_grey</item>
    </style>

    <!-- Style for Button with white text for orange background -->
    <style name="Widget.App.Button" parent="Widget.Material3.Button">
        <item name="android:textColor">@color/white</item>
    </style>

    <!-- Style for OutlinedButton with orange text -->
    <style name="Widget.App.OutlinedButton" parent="Widget.MaterialComponents.Button.OutlinedButton">
        <item name="android:textColor">@color/primary_orange</item>
    </style>

    <style name="ThemeOverlay.CurriculumApp.AlertDialog" parent="ThemeOverlay.AppCompat.Dialog.Alert">
        <item name="android:windowBackground">@color/white</item>
        <item name="android:colorBackground">@color/white</item>
        <item name="colorBackgroundFloating">@color/white</item>
        <item name="android:textColorPrimary">@color/black</item>
        <item name="android:textColorSecondary">@color/text_grey</item>
        <item name="colorAccent">@color/primary_orange</item>
    </style>
</resources>
